<UserControl x:Class="ToolBox_Pro.Views.XmlTagFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolBox_Pro.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20" >
        <TextBlock Text="CSV-Datei mit XML-Zellen filtern" FontWeight="Bold" FontSize="16"/>
        <StackPanel Orientation="Horizontal" >
            <TextBox Width="300" Text="{Binding SelectedFilePath}" IsReadOnly="True"/>
            <Button Content="Durchsuchen" Command="{Binding BrowseFileCommand}"/>
            <Button Content="Laden" Command="{Binding LoadMatchesCommand}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Matches}" AutoGenerateColumns="False" Height="300">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Zeile" Binding="{Binding LineNumber}" Width="50"/>
                <DataGridTextColumn Header="Original" Binding="{Binding OriginalLine}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Ergebnisse exportieren" Command="{Binding ExportMatchesCommand}" />
    </StackPanel>
</UserControl>
