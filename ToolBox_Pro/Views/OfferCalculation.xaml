<UserControl x:Class="ToolBox_Pro.Views.OfferCalculation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewmodels="clr-namespace:ToolBox_Pro.ViewModels"
             xmlns:converters="clr-namespace:ToolBox_Pro.Converters"
             xmlns:local="clr-namespace:ToolBox_Pro.Controls"
             Height="Auto" Width="Auto">
    <UserControl.DataContext>
        <viewmodels:OfferCalculationViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:ZeroToCollapsedConverter x:Key="ZeroToCollapsedConverter"/>
    </UserControl.Resources>


    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        
        <Border Grid.Column="0" Style="{StaticResource ToolboxContainerStyle}">
            <StackPanel>
                <TextBlock Style="{StaticResource SchrittUeberschriftStyle}"
                           Text="Speicherort der Angebote"/>
                <TextBlock Style="{StaticResource  TextBlockStyle}"
                           Text="Bitte auswählen:"/>
                <StackPanel>
                    <Button Style="{StaticResource FlatButtonStyle}"
                            Content="Ordner wählen"
                            Command="{Binding PickFolderCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
        <Border Grid.Column="1" Margin="10 10 0 0" Style="{StaticResource ToolboxContainerStyle}">

            <StackPanel Orientation="Vertical">
                    <TextBlock Style="{StaticResource SchrittUeberschriftStyle}"
           Text="Angebote auswerten"/>
                    <Button Content="Mails speichern" Command="{Binding SaveFilesCommand}"
                        Style="{StaticResource FlatButtonStyle}" Margin="0 10 0 0" MaxHeight="30"/>
                    <Button Content="Angebote auswerten" Command="{Binding AnalyzeOffersCommand}"
                        Style="{StaticResource FlatButtonStyle}" Margin="0 10 0 0" MaxHeight="30"/>
                        <Button Content="Gesamtangebot" Command="{Binding GenerateOfferCommand}"
                        Style="{StaticResource FlatButtonStyle}" Margin="0 10 0 0" MaxHeight="30"/>
                    </StackPanel>
        </Border>
        </Grid>
        <!-- Spinner während Analyse -->
         <local:LoadingSpinner Grid.Row="3" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" />


            <!-- Angebotsliste -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Offers}"
                  AutoGenerateColumns="False"
                  Visibility="{Binding Offers.Count, Converter={StaticResource ZeroToCollapsedConverter}}"
                  Style="{StaticResource ToolboxDataGridStyle}"
                  Margin="0 10 0 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Angebotsdatei" Binding="{Binding FileName}" Width="*"/>
                <DataGridTextColumn Header="Summe" Binding="{Binding Price, StringFormat={}{0:N2} €}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Gesamtsumme -->
        <TextBlock Grid.Row="3"
                   FontSize="16" FontWeight="Bold"
                   HorizontalAlignment="Right"
                   Visibility="{Binding Offers.Count, Converter={StaticResource ZeroToCollapsedConverter}}">
            <TextBlock.Inlines>
                <Run Text="Gesamtsumme: "/>
                <Run Text="{Binding TotalPrice, StringFormat={}{0:N2} €}" />
            </TextBlock.Inlines>
        </TextBlock>
        
    </Grid>
</UserControl>
