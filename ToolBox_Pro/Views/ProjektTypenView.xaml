<UserControl x:Class="ToolBox_Pro.Views.ProjektTypenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolBox_Pro.Views"
             xmlns:vm="clr-namespace:ToolBox_Pro.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:ProjektTypenViewModel/>
    </UserControl.DataContext>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0"
                  ItemsSource="{Binding ProjektTypen}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Style="{StaticResource ToolboxDataGridStyle}"
                  HeadersVisibility="Column"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Typ" Binding="{Binding Typ}" Width="100"/>
                <DataGridTextColumn Header="Weidemann" Binding="{Binding BezWM}" Width="*"/>
                <DataGridTextColumn Header="Wacker Neuson" Binding="{Binding BezWN}" Width="*"/>
                <DataGridTextColumn Header="Kramer Bau" Binding="{Binding BezKramerBau}" Width="*"/>
                <DataGridTextColumn Header="Kramer Land" Binding="{Binding BezKramerLand}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid Grid.Column="2"
                      ItemsSource="{Binding ProjektTypen}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Style="{StaticResource ToolboxDataGridStyle}"
                      HeadersVisibility="Column"
                      CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Typ" Binding="{Binding Typ}" Width="100"/>
                <DataGridTextColumn Header="SOP Nullserie" Binding="{Binding SOPNullserie, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="SOD Nullserie" Binding="{Binding SODNullserie, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="SOP Serie" Binding="{Binding SOPSerie, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="SOD Serie" Binding="{Binding SOPSerie, StringFormat=d}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
