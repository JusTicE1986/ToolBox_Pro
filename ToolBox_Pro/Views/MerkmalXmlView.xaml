<UserControl x:Class="ToolBox_Pro.Views.MerkmalXmlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolBox_Pro.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" Margin="0,0,0,10" Width="200">
        <Button Content="Excel laden" Command="{Binding LadeExcelCommand}" />
        <Button Content="XML laden" Command="{Binding LadeXmlCommand}" />
        <Button Content="Vergleichen" Command="{Binding VergleicheCommand}" />
        <Button Content="Exportieren" Command="{Binding ExportiereCommand}" />
        <Button Content="Reset" Command="{Binding ResetCommand}" />
    </StackPanel>
</UserControl>
