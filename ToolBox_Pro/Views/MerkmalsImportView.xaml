<UserControl x:Class="ToolBox_Pro.Views.MerkmalsImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolBox_Pro.Views"
             xmlns:vm="clr-namespace:ToolBox_Pro.ViewModels"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:MerkmalsImportViewModel/>
    </UserControl.DataContext>
    
    <DockPanel>
        <ComboBox Width="150" Height="25" ItemsSource="{Binding Typen}" SelectedItem="{Binding AusgewaehlterTyp, Mode=TwoWay}" Margin="0 0 20 0"/>
        <ComboBox Width="150" Height="25" ItemsSource="{Binding MerkmalModelle}" SelectedItem="{Binding AusgewaehltesMerkmalModell, Mode=TwoWay}" DisplayMemberPath="ModellBez" Margin="0 0 20 0"/>
        <Button Content="Merkmalsliste laden" Command="{Binding ExcelLadenCommand}" DockPanel.Dock="Top" Padding="10" Margin="10"/>
        <Button Content="Merkmale-XML laden" Command="{Binding LadeXmlMerkmaleCommand}" DockPanel.Dock="Top" Padding="10" Margin="10"/>
        <Button Content="Listen vergleichen" Command="{Binding AbgleichIdsMitXmlCommand}" DockPanel.Dock="Top" Padding="10" Margin="10"/>
        <Button Content="Projektfilter speichern" Command="{Binding ExportiereXmlCommand}" DockPanel.Dock="Top" Padding="10" Margin="10"/>
        <DataGrid ItemsSource="{Binding Eintraege}" AutoGenerateColumns="False" Margin="10" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}"/>
                <DataGridTextColumn Header="ST4 Id" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Merkmal" Binding="{Binding MerkmalNameUndWert}"/>
                <DataGridTextColumn Header="Bezeichnung" Binding="{Binding BezeichMerkmalwert}"/>
            </DataGrid.Columns>
            
        </DataGrid>
        <DataGrid ItemsSource="{Binding XmlMerkmale}" AutoGenerateColumns="False" Margin="10" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Bezeichnung" Binding="{Binding Bezeichnung}" />
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
